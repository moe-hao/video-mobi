export enum AntomNotifyType {
    PaymentResult = 'PAYMENT_RESULT',
    CaptureResult = 'CAPTURE_RESULT',
}

export enum AntomSubscriptionNotificationType {
    Create = 'CREATE',
    Terminate = 'TERMINATE',
    Cancel = 'CANCEL',
}
